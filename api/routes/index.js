const login = require('./login');
const register = require('./register');
const password = require('./password');
const contact = require('./contact');

/**
 * Routes.
 * @module routes
 */
module.exports = [login, register, password, contact];
